node_modules/
.pnpm-store/
.local/
.tmp/
.DS_Store
coverage/
dist/
dist-electron/
out/
playwright-report/
test-results/
.tmp/
*.tsbuildinfo
packages/agent-core/.env.local
.env.local
excalidraw.log
.worktrees/
.claude/worktrees
.claude/worktrees/
.claude/worktrees/**

# Raw protocol captures can include private thread previews, screenshots,
# local paths, and operational data. Commit curated replay fixtures instead.
apps/desktop/e2e/fixtures/**/raw.capture.jsonl

# electron-builder output (signed/notarized DMG + ZIP land here)
apps/desktop/dist/
# pnpm deploy stage (release.mjs materializes a flat node_modules tree here)
apps/desktop/release-stage/

# Release-time secrets — must never land in the repo
.envrc.release
*.p12
AuthKey_*.p8

# PwrAgent design bundle — keep source files (HTML/CSS/JSX/SVG) only.
# Chat transcripts and pasted screenshots can carry private context
# (internal decisions, paths, account names, identifying details);
# never commit them. See docs/design/pwragent-v2/SOURCE.md for the
# re-import procedure.
docs/design/pwragent-v2/chats/
docs/design/pwragent-v2/project/uploads/
