# GitLab brand asset

`tanuki.svg` is the unaltered full-color GitLab mark from
[GitLab's SVG repository](https://gitlab.com/gitlab-org/gitlab-svgs/-/blob/main/illustrations/gitlab_logo.svg).
Downloaded from the corresponding raw URL on 2026-09-11.

The mark identifies the GitLab CLI (`glab`). Render it without recoloring,
cropping, or effects; it does not imply endorsement by GitLab.
